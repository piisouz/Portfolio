[1mdiff --git a/src/journey.cpp b/src/journey.cpp[m
[1mindex eddb580..7146742 100644[m
[1m--- a/src/journey.cpp[m
[1m+++ b/src/journey.cpp[m
[36m@@ -1,6 +1,8 @@[m
 #include "journey.h"[m
[31m-#include <stdexcept>[m
[32m+[m
 #include <cmath>[m
[32m+[m[32m#include <algorithm>[m
[32m+[m[32m#include <stdexcept>[m
 [m
 namespace GPS[m
 {[m
[36m@@ -8,145 +10,90 @@[m [mnamespace GPS[m
 Journey::Journey(std::vector<TimedWaypoint> timedWaypoints) : points{timedWaypoints} {}[m
 [m
 [m
[31m-// TODO: Stub definition needs implementing[m
 metres Journey::netHeightGain() const[m
 {[m
[31m-    //makes the requirement of having at least two waypoints[m
[31m-    if (points.size() < 2)[m
[31m-    {[m
[31m-        throw std::domain_error("A journey must contain at least two waypoints to calculate height gain.");[m
[31m-    }[m
[31m-[m
[31m-    //calculate the difference between the final altitude and starting altitude[m
[32m+[m[32m    if (points.size() <= 2) throw std::runtime_error("Not enough waypoints!");[m
 [m
[31m-    metres startAltitude = points.front().waypoint.altitude();[m
[31m-    metres endAltitude = points.back().waypoint.altitude();[m
[31m-    metres difference = endAltitude - startAltitude;[m
[32m+[m[32m    metres verticalChange = points.back().waypoint.altitude() - points.front().waypoint.altitude();[m
 [m
[31m-    // Return the difference if positive if not returns 0 [m
[31m-    if (difference < 0.0)[m
[32m+[m[32m    if (verticalChange >= 1)[m
     {[m
[31m-        return 0.0;[m
[32m+[m[32m        return verticalChange;[m
[32m+[m[32m    }[m
[32m+[m[32m    else[m
[32m+[m[32m    {[m
[32m+[m[32m        throw std::runtime_error("No height gain!");[m
     }[m
[31m-[m
[31m-    return difference;[m
 }[m
 [m
[31m-// TODO: Stub definition needs implementing[m
 metres Journey::totalHeightGain() const[m
 {[m
[31m-    //makes the requirement of having at least two waypoints[m
[31m-    if (points.size() < 2)[m
[31m-    {[m
[31m-        throw std::domain_error("A journey must contain at least two waypoints to calculate total height gain.");[m
[31m-    }[m
[32m+[m[32m    if (points.size() <= 2) throw std::runtime_error("Not enough waypoints!");[m
 [m
[31m-    metres totalGain = 0.0;[m
[32m+[m[32m    metres total = 0;[m
 [m
[31m-    //loops through points and sum up the altitude changes[m
[31m-    for (size_t i = 1; i < points.size(); ++i)[m
[32m+[m[32m    for (unsigned int current = 0, next = 1; next < points.size() ; ++current, ++next)[m
     {[m
[31m-        metres prevAltitude = points[i - 1].waypoint.altitude();[m
[31m-        metres currAltitude = points[i].waypoint.altitude();[m
[31m-        metres difference = currAltitude - prevAltitude;[m
[31m-[m
[31m-        if (difference > 0.0)[m
[31m-        {[m
[31m-            totalGain += difference;[m
[31m-        }[m
[32m+[m[32m        metres verticalChange = points[next].waypoint.altitude() - points[current].waypoint.altitude();[m
[32m+[m[32m        if (verticalChange >= 1) total += verticalChange;[m
     }[m
 [m
[31m-    return totalGain;[m
[32m+[m[32m    if (total <= 0) throw std::runtime_error("No height gain!");[m
[32m+[m
[32m+[m[32m    return total;[m
 }[m
 [m
[31m-// TODO: Stub definition needs implementing[m
 metresPerSecond Journey::maxSpeed() const[m
 {[m
[31m-    // makes the requirement of at least 2 waypoints[m
[31m-    if (points.size() < 2)[m
[31m-    {[m
[31m-        throw std::domain_error("A journey must contain at least two waypoints to calculate max speed.");[m
[31m-    }[m
[32m+[m[32m    if (points.size() <= 2) throw std::runtime_error("Cannot measure speed in a journey of two or fewer points.");[m
 [m
[31m-    metresPerSecond maximumSpeed = 0.0;[m
[32m+[m[32m    metresPerSecond maximumSpeed = 0;[m
 [m
[31m-    //loops through points[m
[31m-    for (size_t i = 1; i < points.size(); ++i)[m
[32m+[m[32m    for (unsigned int current = 1, next = 2; next < points.size()-1 ; ++current, ++next)[m
     {[m
[31m-        const TimedWaypoint& prev = points[i - 1];[m
[31m-        const TimedWaypoint& curr = points[i];[m
[31m-[m
[31m-        //does the time validation[m
[31m-        std::time_t timeDiff = curr.timeStamp - prev.timeStamp;[m
[31m-        if (timeDiff <= 0)[m
[31m-        {[m
[31m-            throw std::domain_error("Time elapsed between adjacent points must be greater than zero.");[m
[31m-        }[m
[32m+[m[32m        metres horizontalDifference = Waypoint::horizontalDistanceBetween(points[current].waypoint,points[next].waypoint);[m
[32m+[m[32m        metres verticalDifference = Waypoint::verticalDistanceBetween(points[current].waypoint,points[next].waypoint);[m
[32m+[m[32m        metres distanceBetweenPoints = std::hypot(horizontalDifference,verticalDifference);[m
 [m
[31m-        // calculates the 3D distance[m
[31m-        metres hDist = Waypoint::horizontalDistanceBetween(prev.waypoint, curr.waypoint);[m
[31m-        metres vDist = Waypoint::verticalDistanceBetween(prev.waypoint, curr.waypoint);[m
[31m-        metres totalDist = std::sqrt(std::pow(hDist, 2) + std::pow(vDist, 2));[m
[32m+[m[32m        seconds timeBetweenPoints = points[next].timeStamp - points[current].timeStamp;[m
[32m+[m[32m        if (timeBetweenPoints <= 0) throw std::runtime_error("Journey contains time periods that are not strictly positive.");[m
 [m
[31m-        //calculates speed[m
[31m-        metresPerSecond speed = totalDist / static_cast<double>(timeDiff);[m
[32m+[m[32m        metresPerSecond currentSpeed = distanceBetweenPoints / timeBetweenPoints;[m
 [m
[31m-        //tracks the maximum[m
[31m-        if (speed > maximumSpeed)[m
[31m-        {[m
[31m-            maximumSpeed = speed;[m
[31m-        }[m
[32m+[m[32m        maximumSpeed = std::max(currentSpeed,maximumSpeed);[m
     }[m
 [m
     return maximumSpeed;[m
 }[m
 [m
[31m-// TODO: Stub definition needs implementing[m
 Waypoint Journey::highestWaypoint() const[m
 {[m
[31m-    //throws domain error if there are no waypoints in the journey[m
[31m-    if (points.empty())[m
[31m-    {[m
[31m-        throw std::domain_error("Cannot determine the highest waypoint of an empty journey.");[m
[31m-    }[m
[31m-[m
[31m-    //tracks the highest waypoint found [m
[31m-    Waypoint highest = points.front().waypoint;[m
[32m+[m[32m    if (points.size() <= 1) throw std::runtime_error("Not enough waypoints!");[m
 [m
[31m-    //loop through and find to maximum altitude[m
[31m-    for (size_t i = 1; i < points.size(); ++i)[m
[32m+[m[32m    Waypoint highestPointSoFar = points[1].waypoint;[m
[32m+[m[32m    for (unsigned int current = 1; current < points.size()-1; ++current)[m
     {[m
[31m-        if (points[i].waypoint.altitude() > highest.altitude())[m
[32m+[m[32m        if (points[current].waypoint.altitude() > highestPointSoFar.altitude())[m
         {[m
[31m-            highest = points[i].waypoint;[m
[32m+[m[32m            highestPointSoFar = points[current].waypoint;[m
         }[m
     }[m
[31m-[m
[31m-    return highest;[m
[32m+[m[32m    return highestPointSoFar;[m
 }[m
 [m
[31m-// TODO: Stub definition needs implementing[m
 Waypoint Journey::lowestWaypoint() const[m
 {[m
[31m-    //throw domain error if there are no waypoints in the journey[m
[31m-    if (points.empty())[m
[31m-    {[m
[31m-        throw std::domain_error("Cannot determine the lowest waypoint of an empty journey.");[m
[31m-    }[m
[31m-[m
[31m-    //tracks the lowest waypoint found [m
[31m-    Waypoint lowest = points.front().waypoint;[m
[32m+[m[32m    if (points.size() <= 1) throw std::runtime_error("Not enough waypoints!");[m
 [m
[31m-    //loop through and find the minimum altitude[m
[31m-    for (size_t i = 1; i < points.size(); ++i)[m
[32m+[m[32m    Waypoint lowestPointSoFar = points[1].waypoint;[m
[32m+[m[32m    for (unsigned int current = 1; current < points.size()-1; ++current)[m
     {[m
[31m-        if (points[i].waypoint.altitude() < lowest.altitude())[m
[32m+[m[32m        if (points[current].waypoint.altitude() < lowestPointSoFar.altitude())[m
         {[m
[31m-            lowest = points[i].waypoint;[m
[32m+[m[32m            lowestPointSoFar = points[current].waypoint;[m
         }[m
     }[m
[31m-[m
[31m-    return lowest;[m
[32m+[m[32m    return lowestPointSoFar;[m
 }[m
 [m
 [m
