#include <iostream>

main(){

        double x = 3.14;
        double y = 4;
        double z;
        
        //z = std:max(x, y); This states that we use z for the answers adn it basically chooses the maxiumum number between x and y
        //z = std.min(x, y); The same but min number
        //z = pow(2, 4); This does the power of two to the power of 4
        //z = sqrt(9); does square root
        //z = round(x); rounds the number
        //z = ceil(x); always rounds up
        //z = floor(x); always rounds down

    return 0;
}